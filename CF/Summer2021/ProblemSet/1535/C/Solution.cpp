#include <iostream>
#include <fstream>
#include <string>

int main () {
    #ifndef ONLINE_JUDGE
    std::ifstream InputFile("Input.txt");
    std::cin.rdbuf(InputFile.rdbuf());
    #endif

    int NumTestCases;
    std::cin >> NumTestCases;

    for(int Counter = 0; Counter < NumTestCases; Counter++){
        std::string UnstableString;

        
    }

    return 0;
}